// Numeric spacing scale, extracted from the literal pixel values already in
// use across apps/web and apps/mobile — adopting this is a no-op visually.
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 24,
  xxl: 32,
} as const;
