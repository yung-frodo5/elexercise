# Used by SupabaseSessionRepository. The service-role key is a server-side
# secret — it bypasses RLS, so never expose it to a client app. Do not commit
# a real .env with these filled in.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
